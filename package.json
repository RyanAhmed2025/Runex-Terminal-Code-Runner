{
  "name": "runex",
  "version": "0.1.0",
  "description": "Universal Runtime CLI Sandbox — Paste. Run. Observe. Exit. Clean.",
  "type": "module",
  "bin": {
    "runex": "./bin/runex"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^11.1.0",
    "esbuild": "^0.19.0",
    "execa": "^8.0.1",
    "inquirer": "^9.2.11",
    "ora": "^7.0.1",
    "tsx": "^4.6.2"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["cli", "sandbox", "repl", "runtime", "executor"],
  "license": "MIT"
}
